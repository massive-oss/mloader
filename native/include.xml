<?xml version="1.0" encoding="utf-8"?>
<project>
	
	<ndll name="mloader" />

	<haxelib name="inthebox-macros"/>
	
    <dependency name="MobileCoreServices.framework" if="ios" />
    <dependency name="Security.framework" if="ios" />
    <dependency name="SystemConfiguration.framework" if="ios" />

	<!-- Use the following for an Android Java extension, not needed otherwise -->
	
	<!-- <dependency name="mloader_native" path="dependencies/android" if="android" /> -->
	<!-- <android extension="org.haxe.extension.Mloader_native" /> -->
	
</project>
